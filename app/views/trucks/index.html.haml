%form#search{ role: "form" }
  .form-group
    %label.sr-only{ for: "search" } Enter a street address and press return
    %input#search.form-control{ type: "text", placeholder: "Enter a street address and press return" }
#map
- content_for(:truck_data) do
  %script= raw "sfft.data = #{@trucks.to_json};"
